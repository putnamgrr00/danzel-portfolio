# Phase 2 — Notes for executor

Use these when implementing the plan in `phase_2_layout_restructure_5d039af5.plan.md`.

---

## Task 2 — Things to watch after it runs

1. **Content & Social row span**  
   Ensure the center tile (Content & Social) actually spans two rows visually and is not squashed. If it looks equal height to the other tiles, treat that as the first fix for Phase 3 (e.g. explicit row height or `grid-rows`).

2. **isFeatured logic**  
   Using index 1 (Content & Social) as "featured" for extra padding is desired — the biggest tile should feel more spacious. Confirm that "featured" does **not** add a different background color that breaks the dark premium look. Padding only.

---

## Task 3 — Proof section button

- **One button only** in the proof section: **"View real work + metrics →"** (URL: `https://drive.google.com/drive/folders/1DKI0iZyt4sNhMMldwLg59v8pf0lUwO4i?usp=sharing`).
- Do **not** add a "View resume" button there. The resume link is already in the Final CTA section as "Download CV"; no duplication in the proof section.

---

Update the main plan file with these points if you have write access to it; otherwise follow this addendum when executing.
